import type { Metadata } from 'next';import { VoiceStudio } from '@/components/VoiceStudio';
export const metadata:Metadata={title:'Voice Studio'};export default function Page(){return <VoiceStudio/>}
