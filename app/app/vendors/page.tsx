import type { Metadata } from 'next';import { VendorDashboard } from '@/components/VendorDashboard';
export const metadata:Metadata={title:'Vendor Operations'};export default function Page(){return <VendorDashboard/>}
